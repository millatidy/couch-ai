# couch-ai

> couch-backend

## Build Setup

``` bash
# install dependencies
pip install requirements.txt

# serve with hot reload at localhost:5000
python run0.py

# please note debug must always be set to False in run0.py
if __name__ == "__main__":
	app.run(debug=False)

# how to make things not work, set debug=True in run0.py
if __name__ == "__main__":
	app.run(debug=True)
